<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="noindex">
    <meta http-equiv="refresh" content="0;url={{ $appUrl }}">
    <title>Redefinir senha</title>
    <style>
        body { font-family: system-ui, sans-serif; background: #0B0D0F; color: #f2f4f5; margin: 0; padding: 24px; display: grid; place-items: center; min-height: 100vh; text-align: center; }
        main { max-width: 340px; }
        a.button { display: block; background: #2dd4bf; color: #0B0D0F; padding: 14px; border-radius: 999px; font-weight: 600; text-decoration: none; margin: 24px 0 16px; }
        a.link { color: #9aa4ad; font-size: 14px; }
    </style>
</head>
<body>
<main>
    <h1>Redefinir senha</h1>
    <p>Abrindo o app CestaZen…</p>
    <a class="button" href="{{ $appUrl }}">Abrir no app</a>
    <a class="link" href="{{ $webUrl }}">Não tenho o app — redefinir pelo site</a>
</main>
</body>
</html>
